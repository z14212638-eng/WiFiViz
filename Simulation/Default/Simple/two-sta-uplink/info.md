# Two STA Uplink

A compact Wi-Fi scenario with one AP and two STAs. Both STAs send UDP uplink
traffic to the AP so the timeline contains overlapping transmissions from
multiple stations.

Useful command:

```bash
./ns3 run "two-sta-uplink --enable-wifiviz=1 --launch-viewer=1"
```
